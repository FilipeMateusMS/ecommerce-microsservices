eureka:
  instance:
    hostname: localhost
  # Configurações register-with-eureka e fetch-registry por padrão são true
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
server:
  port: 8761